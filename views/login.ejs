<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-800 min-h-screen text-white flex items-center justify-center px-4">

  <div class="w-full max-w-md bg-zinc-900 border-2 border-slate-400 rounded-xl p-8 shadow-xl space-y-6">

    <!-- Home Button -->
    <div class="text-center">
      <a href="/" class="inline-block px-4 py-2 bg-blue-600 hover:bg-blue-700 rounded-md font-semibold transition duration-200">
        Home
      </a>
    </div>

    <!-- Login Form -->
    <form action="loggedin" method="post" class="space-y-4">
      <input
        type="text"
        name="email"
        placeholder="Enter Email"
        class="w-full px-4 py-3 rounded-md bg-zinc-700 border border-slate-500 outline-none focus:ring-2 focus:ring-blue-500 transition duration-150"
      />
      <input
        type="password"
        name="password"
        placeholder="Enter Password"
        class="w-full px-4 py-3 rounded-md bg-zinc-700 border border-slate-500 outline-none focus:ring-2 focus:ring-blue-500 transition duration-150"
      />
      <input
        type="submit"
        value="Login"
        class="w-full px-4 py-3 bg-green-600 hover:bg-green-700 rounded-md font-semibold cursor-pointer transition duration-200"
      />
    </form>

  </div>
</body>
</html>